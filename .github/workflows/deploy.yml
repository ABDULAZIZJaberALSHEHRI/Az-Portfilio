name: OCI Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Run local deploy script
        run: |
          cd /home/opc/Abdulaziz-portfilio
          ./deploy.sh
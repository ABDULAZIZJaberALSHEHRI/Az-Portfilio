version: '3.8'

services:
  portfolio:
    build: .
    container_name: az-portfolio-app
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - portfolio-data:/app/instance
    environment:
      - DATABASE_URL=/app/instance/database.db
      - FLASK_SECRET_KEY=8c39c7f21688b5a8b0829b1d3bce4c3867f4f0880728f1d8
      - ADMIN_PASSWORD=Aa-251436


volumes:
  portfolio-data:
